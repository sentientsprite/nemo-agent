{
  "name": "spryte-engine",
  "version": "0.1.0",
  "description": "Spryte Engine — Modular AI agent framework",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "rm -rf packages/*/dist",
    "test": "echo 'Tests not implemented yet'"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.0.0"
}
