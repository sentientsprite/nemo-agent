---
summary: "CLI reference for `nemo uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `nemo uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
nemo uninstall
nemo uninstall --all --yes
nemo uninstall --dry-run
```
